<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/index.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito&family=Quicksand:wght@700&display=swap" rel="stylesheet">
    <title>404</title>

</head>
<header class="top-bar">


</header>

<body>
    <%- include ("fragments/navbar")%>
        <main>
            <div class="container title-container">
                <h3>
                    <%= question.title %>
                </h3>

            </div>
            <h7>
                vista
                <%= question.views %> veces
            </h7>
            <h7>
                <%= question.likes-question.dislikes %> votos
            </h7>

            <p>Formulada
                <%=question.dateAgo%>
                    por
                    <a href="/users/<%= question.id_user %> ">
                        <%= question.name %>
                    </a>
            </p>
            <img alt="Foto de perfil de <%=question.name %>" src="/images/<%= question.image %> " class="profile-img">
            <h5>
                <%= question.body %>
            </h5>

            <ul class="container tags-container">
                <% if (question.tags) { %>
                    <% question.tags.forEach(tag => {%>
                        <li class="tag-element">
                            <a href="/tag/<%=tag.id%>">
                                <%=tag%>
                            </a>
                        </li>
                        <%}); %>
                            <% } %>

            </ul>
            <ul class="container questions-container">
                <% answers.forEach(a =>{ %>
                    <li class="row question-element">
                        <div class="col-4 question-extradata">
                            <p>Formulada
                                <%=a.dateAgo%>
                                    por
                                    <a href="/users/<%= a.user_id %> ">
                                        <%= a.name %>
                                    </a>
                            </p>

                            <img alt="Foto de perfil de <%=a.name %>" src="/images/<%= a.image %> " class="profile-img">
                        </div>
                        <div class="col-8 question-data">
                            <p>
                                <%= a.body %>
                            </p>
                        </div>
                    </li>
                    <% }); %>
            </ul>


        </main>


</body>

</html>